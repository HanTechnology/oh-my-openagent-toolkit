---
name: frontend-design
description: "Deprecated upstream wrapper kept for completeness. Not a primary route. Use `impeccable` instead for design direction and refinement in the local bundle."
---

# Frontend Design

This deprecated upstream wrapper is included for completeness only within the local `impeccable` family.

Use `impeccable` instead. In this local bundle:
- `frontend-web` or `mobile-app` stay the primary implementation packs,
- `impeccable` provides the companion design direction and anti-slop layer,
- and this wrapper is not a primary route.

Keep `frontend-design` present for completeness only. Do not perform design work from `frontend-design`; redirect to `impeccable`.
